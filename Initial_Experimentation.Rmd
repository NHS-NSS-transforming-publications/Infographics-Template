---
output:
  html_document:
    fig_width: 3
  pdf_document: default
  word_document: default
---


```{r, echo=FALSE, results = 'hide', message = FALSE, warnings = FALSE}
#This area creates the plots and sets up other objects to be inserted into the RMarkdown code later on.

image_width <- 200
fontSize <- 24

#-----
df <- data.frame(
  group = c("Yes", "No"),
  value = c(25, 75)
)
head(df)

suppressWarnings(  library(ggplot2)   ) #Load library ggplot and suppress any warnings that pop up.
                                        #I had difficulty suppressing them purely with setting warnings = False
                                        #at the start of the RMarkdown chunk.


# Sample Barplot
bp <- ggplot(df, aes(x="", y=value, fill=group)) +
            geom_bar(width = .35, stat = "identity", fill = c('red','orange'))

bp <- bp + labs(x="", y="")  #This removes the 'x' and 'value' labels for the x and y axes respectively.

bp +  theme(legend.position = "none",  #Trying to set the font size to 2 to minimize the space the Y-Axis takes up.
            axis.text.y = element_text(size=2) #erase later, it doesn't seem like this line works.
            )

pie <- bp + #geom_text(aes(label = '14%'), vjust = "inward", hjust = "inward") +
         coord_polar("y", start=0*pi/(180)) +  #This changes the barplot into a donut plot.
         annotate("text", x = 0, y = 0, label = "25%", size = 10, col = 'orange')


pie <- pie + theme(panel.background = ggplot2::element_blank(),
                   axis.text.x = ggplot2::element_blank(),  #This will get rid of the x-axis labels.
                   axis.ticks=element_blank())  #This will get rid of the single x-axis tick.
#-----



```

```{r, out.width="50%", out.extra='style="background-color: #9ecff7; padding:10px; display: inline-block;"', echo=FALSE}
plot(10:1, pch=21, bg="red")
plot(1:10, pch=21, bg="red")
pie
pie

```


<!-- ![Pie Chart](pie.png) \ -->  <!-- this line imported the pie chart back in after a previous line exported it -->


```{r, out.width="50%", out.extra='style="background-color: #9ecff7; padding:10px; display: inline-block;"', echo=FALSE}
plot(1:10, pch=21, bg="red")
```


<span style = "margin-left: 40px; display: inline-block; width: 600px;">
```{r, out.width="50%", out.extra='style="background-color: #9ecff7; padding:10px; display: inline-block;"', echo=FALSE}
plot(1:10, pch=21, bg="red")
```


<!-- #save the donut plot as a png. -->
<!-- pie + ggsave("pie.png", width = 3, height = 3) -->
<!-- getwd() -->
<!-- <img src="C:/Users/roryma01/Downloads/National-Stats-Template-filesize/pie.png" alt="" width = "200"> </img> -->
<img src="pie.png" alt="" width = `r image_width`> </img>
<span style="font-size: `r fontSize`px;">The 3rd one.</span> </span>



<span style = "margin-left: 40px; display: inline-block; width: 100px;">This is a sentence. 
This is the 2nd line. 
The 3rd one. </span>
```{r, out.width="40%", out.extra='style="background-color: #9ecff7; padding:10px; display: inline-block;"', echo=FALSE}
print('This is a sentence!')
```


![National Statistics Quality Mark](kitemark_tcm97-17949.jpg) \  <!-- showing how to import an external image -->




**Tasks and Issues/Questions to Resolve:** <br> <!-- the <br> (break) tags cause the text to go to the next line -->
1.  Minimize y-axis values in donut plot.   ##Can't seem to minimize the space using any theme() settings. <br>
2.  Understand how divs, spans, and inline-block works together. <br>
3.  See if the donut plot can be put directly into a span within the page or if it 
    has to be saved as a png and then reimported back in. ##It can be put directly in! <br>
4.  Figure out best way of displaying images, plots, and text besides one another
    in a grid like setup.  <br>
5.  Figure out where ggsave saves the png file.  ##It looks like it saves it in the working directory!  <br>






